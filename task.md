# 人机协同四格漫画生成系统 (HITL Yon Koma) - AI 开发任务清单

## 🎯 核心架构原则（AI 开发者必读）
1. **绝对的人机协同 (Strict HITL)：** 系统在每一个生成节点（角色图、剧本、分镜、单格草图、最终渲染）必须暂停，等待人类用户的 Explicit 确认或修改，**禁止任何形式的 AI 自动重试循环**。
2. **垫图驱动 (Reference-Driven)：** 图像生成强依赖预先生成的“角色规范设定图”作为垫图（Image Prompt / ControlNet / IP-Adapter 等机制），以确保角色一致性。
3. **环境规范：** 在编写服务器配置或部署脚本（如修改 `.env` 或 `docker-compose.yml`）时，所有终端编辑命令请使用 `vim`。

---

## 📋 执行清单 (Step-by-Step)

### Step 1: 项目脚手架与基础模型搭建
**目标：建立前后端分离的运行环境和基础数据库结构。**

- [ ] **Task 1.1: 初始化后端 (Go + Gin)**
  - 创建 Go 项目目录结构（`cmd`, `internal/api`, `internal/service`, `internal/models`）。
  - 引入 Gin 框架并配置基础路由和跨域 (CORS) 中间件。
- [ ] **Task 1.2: 初始化前端 (React + Vite + TypeScript)**
  - 创建 React 项目，配置 Axios 拦截器用于 API 通信。
  - 配置状态管理 (Zustand 或 Redux) 和基础路由。
- [ ] **Task 1.3: 数据库 Schema 定义 (SQLite 默认)**
  - 创建 `users`, `projects`, `characters`, `scripts`, `storyboards`, `panels` 表。
  - **重点**：在 `characters` 表中增加 `reference_sheet_url` 字段，专门用于存放作为垫图的规范设定图。
- [ ] **Task 1.4: 核心接口抽象**
  - 定义 `LLMTextClient` 接口（用于剧本/分镜生成，如 Gemini）。
  - 定义 `LLMImageClient` 接口（用于生图，如 Nano Banana 2），接口参数必须包含 `ReferenceImage` 和 `ImagePromptWeight` 字段。

### Step 2: 角色资产中心与规范图生成系统
**目标：构建角色库，并利用 AI 生成标准的角色设定图作为后续的垫图素材。**

- [ ] **Task 2.1: 角色基础 CRUD API 与 UI**
  - 实现角色文本信息（姓名、外貌描述 Prompt、性格）的创建与编辑。
  - 构建前端的角色管理列表和详情页。
- [ ] **Task 2.2: 规范设定图生成流程 (HITL 节点 1)**
  - **后端**：实现一个 API，接收角色描述 Prompt，调用 `LLMImageClient` 生成“角色三视图/标准立绘”风格的图片。
  - **前端**：在角色详情页增加“生成规范设定图”按钮。
  - **HITL**：图片生成后不直接保存，展示在 UI 上。用户点击“满意并设为垫图”后，才将其路径存入 `characters.reference_sheet_url`；用户也可选择“重新生成”。
- [ ] **Task 2.3: 角色资产存储中间件**
  - 实现本地文件系统或 OSS 上传逻辑，持久化用户确认的规范设定图。

### Step 3: 剧本与分镜的协同创作管线
**目标：实现从灵感到分镜的文本生成，每一步都必须由人类确认和精修。**

- [ ] **Task 3.1: 剧本生成与确认 (HITL 节点 2)**
  - **后端**：编写 Prompt 模板，接收用户“一句话灵感”和选择的角色，调用大语言模型生成包含 4 个起承转合节点的 JSON 剧本草案。
  - **前端**：实现剧本编辑器 UI。将生成的 JSON 渲染为可编辑的文本框。用户修改台词或动作后点击“确认剧本，进入分镜”。
- [ ] **Task 3.2: 分镜生成与确认 (HITL 节点 3 - Previz)**
  - **后端**：接收确认后的剧本，调用大语言模型将其转换为极其详细的分镜渲染参数 JSON（包含每格的景别、光影、画面正向/反向 Prompt）。
  - **前端**：实现聊天式预览 (Previz) 界面。左侧显示分镜参数表单，右侧用类似微信聊天的气泡展示对话。
  - **HITL**：用户可在此界面微调某一个格子的画面 Prompt 或台词。点击“确认分镜，开始绘制”向下推进。

### Step 4: 垫图驱动的渲染引擎
**目标：将人类确认后的分镜和角色规范图结合，逐格生成画面。**

- [ ] **Task 4.1: 渲染任务组装器**
  - 编写后端逻辑：遍历分镜 JSON 的四个格子。对于每个格子中出现的角色，查询数据库提取其 `reference_sheet_url`（设定图路径）。
  - 将分镜的文字 Prompt 与提取到的垫图组合，构建出完整的生图 Payload。
- [ ] **Task 4.2: 单格画面生成**
  - 依次调用图像模型（如 Nano Banana 2 的图生图/图像 Prompt 功能），生成**无字净版 (Clean Plate)** 画面的四格漫画底图。
- [ ] **Task 4.3: 画面质量验收验收 (HITL 节点 4)**
  - **前端**：构建一个“渲染验收台” UI，展示生成的四张单格图片。
  - **HITL**：人类用户对这四格进行视觉检查。如果某格画面崩坏（如手指错误或垫图不生效），用户点击该格下方的“仅重绘本格”。后端只针对该格重新发起 API 请求。只有当用户确认四格全部 OK 后，点击“进入排版”。

### Step 5: 排版合成与输出
**目标：将无字画面与剧本台词合成，生成最终成品。**

- [ ] **Task 5.1: 嵌字坐标生成**
  - 编写排版算法或使用简单的大模型视觉分析，根据气泡类型需求（对话、内心独白、大叫）在单格图片上划定合理的文字框坐标区域。
- [ ] **Task 5.2: 图像合成 (Compositor)**
  - 使用后端图像处理库（如 Go 的 `gg` 或 `freetype`）或前端 Canvas，在无字净版图片上叠加白色气泡图层和黑色文本。
  - 将单格图片按照 2x2 或 1x4 的模板拼接成一张完整的长图或方图。
- [ ] **Task 5.3: 成品导出**
  - 提供高清原图下载 API。